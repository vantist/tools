# Example Tool

這是一個範例工具，用於展示如何在此工具集中建立新工具。

## 使用方式

```bash
cargo run -p example-tool
```

## 開發

此工具作為模板，你可以：
1. 複製此資料夾結構
2. 修改 `Cargo.toml` 中的工具名稱
3. 實作你的工具邏輯在 `src/main.rs`
4. 在根目錄執行 `cargo build` 來編譯
